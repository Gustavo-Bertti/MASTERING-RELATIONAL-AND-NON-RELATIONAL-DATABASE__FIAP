SET SERVEROUTPUT ON

/*
DECLARE
	V_DATE DATE;
BEGIN
	SELECT FIRST_DATE INTO V_DATE FROM EMPLOYEES
	WHERE EMPLOYEE_ID = 100;
END;
*/

DECLARE
	V_DATE DATE;
BEGIN
	SELECT SYSDATE INTO V_DATE FROM DUAL;
	DBMS_OUTPUT_LINE('The first day in university' || V_DATE);
	BEGIN
		SELECT SYSDATE INTO V_DATE FROM DUAL;
		DBMS_OUTPUT.PUT_LINE('The First rotina');
	END;

EXCEPTION
	WHEN OTHERS THEN DBMS_OUTPUT.PUT_LINE('OCORREU UM ERRO, DURANTE A EXECUÇÃO');

END;